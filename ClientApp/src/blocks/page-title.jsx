import React from "react";

export const PageTitle = ({ children }) => (
  // <!-- Hero -->
  <div className="section section-header pb-0">
    <div className="container"></div>
  </div>
  //  <!-- End of Hero section -->
);
